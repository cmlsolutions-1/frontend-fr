'use server'

export const login = async (email: string, password: string) => {
  // Simula inicio de sesión exitoso (puedes validar si quieres)
  console.log('Usuario autenticado:', email);
  return true;
};
